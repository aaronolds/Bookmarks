source "https://rubygems.org"

# Use the same gem set GitHub Pages uses in production, so local previews match.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-readme-index"
end
